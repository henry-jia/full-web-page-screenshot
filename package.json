{
  "name": "full-web-page-screenshot",
  "version": "0.1.0",
  "private": true,
  "license": "MPL-2.0",
  "description": "A dependency-free Firefox extension that captures complete scrollable web pages.",
  "scripts": {
    "build": "pwsh -NoProfile -File scripts/build-release.ps1",
    "test": "node --test",
    "check": "node --check extension/capture-plan.js && node --check extension/i18n.js && node --check extension/annotate.js && node --check extension/background.js && node --check extension/content-script.js && node --check extension/popup/popup.js && node --check extension/editor/editor.js"
  },
  "engines": {
    "node": ">=20"
  }
}
