# Tech Stack

- Firefox WebExtensions, Manifest V2, Firefox 142+
- Native `browser.tabs`, `browser.downloads`, and message-passing APIs
- Plain JavaScript with strict mode; no runtime package dependencies
- HTML Canvas for local bitmap stitching
- Node.js 20+ built-in test runner for deterministic core tests
