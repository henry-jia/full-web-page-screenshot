# Test Plan

## Automated

- Normalize invalid or fractional document geometry.
- Produce one segment for a viewport-sized page.
- Tile pages in both axes and crop right/bottom remainder segments.
- Reject invalid geometry and unsafe output pixel budgets.
- Sanitize filenames and map internal errors to user-facing copy.

### Latest result — 2026-08-25

- Capture planner behavior assertions: 6 passed, 0 failed.
- JavaScript syntax parsing: 5 files passed.
- Manifest JSON and six referenced extension resources: valid.
- Permission boundary: only `activeTab` and `downloads`.
- Network API scan: 0 `fetch`, `XMLHttpRequest`, `WebSocket`, or `sendBeacon` calls.
- Popup binding check: 7 DOM bindings present; live region and progressbar semantics present.

## Manual Firefox smoke test

Status: pending on an interactive Firefox window.

1. Load `extension/manifest.json` as a temporary add-on.
2. Capture a short page and confirm one PNG downloads.
3. Capture a long page containing a fixed header and confirm the header is not repeated.
4. Capture a page wider than the viewport and confirm the right edge is present.
5. Confirm the original scroll position is restored.
6. Try `about:addons` and confirm a clear unsupported-page error appears.

